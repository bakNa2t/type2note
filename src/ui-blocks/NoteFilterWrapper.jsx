import styled from "styled-components";

const NoteFilterWrapper = styled.div`
  width: 100%;
  margin-top: 1rem;
  padding: 1rem 0;
  border-radius: 1rem;
  background-color: var(--color-ebony-800);
`;

export default NoteFilterWrapper;
